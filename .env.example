# PostgreSQL connection used by Flask-SQLAlchemy.
# The application also accepts postgresql:// and normalizes it to psycopg.
DATABASE_URL=postgresql+psycopg://postgres:yoon@localhost/newborn_health

# Use a long random value in real deployments.
SECRET_KEY=replace-with-a-long-random-secret
